{
  "name": "@lib/shared",
  "private": false,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm --out-dir dist",
    "dev": "pnpm build",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "echo \"(lib lint via root)\"",
    "format": "echo \"(lib format via root)\"",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.0.5"
  }
}
